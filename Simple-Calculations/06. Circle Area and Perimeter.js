function perimeterAreaCircel ([radius]){
    let r = radius;
    let area = Math.PI * r * r;
    let perimeter = 2 * Math.PI * r;
    console.log("Area = " + area);
    console.log("Perimeter = " + perimeter);
}
